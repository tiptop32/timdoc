# Customer store

Локальный SQLite-справочник хозяйств и настроек исполнителя.
Тесты: `uv run pytest services/customer-store/tests --no-cov`.
Оценка нормализации: `uv run pytest services/customer-store/evals -m eval --no-cov`.
