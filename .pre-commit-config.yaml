repos:
  - repo: local
    hooks:
      - id: ruff-check
        name: ruff check
        entry: uv run ruff check
        language: system
        types: [python]
      - id: ruff-format
        name: ruff format
        entry: uv run ruff format --check
        language: system
        types: [python]
      - id: gate-tests
        name: gate tests
        entry: uv run pytest -m not\ eval -q --no-cov
        language: system
        pass_filenames: false
        always_run: true
